# Convex
CONVEX_URL=https://your-deployment.convex.cloud
# Optional: Convex CLI deploy/run access for CI/non-interactive use
# CONVEX_DEPLOY_KEY=convex_deploy_key_here

# GitHub Personal Access Token (set in Convex env, not required locally)
# Create at: https://github.com/settings/tokens
# Required scopes: repo (read access to public/private repos)
# GITHUB_TOKEN=github_pat_your-token
